complete -c trunk -e
complete -c trunk -a change -d "Change the trunk branch" -f
complete -c trunk -a status -d "Show the current trunk branch" -f
complete -c trunk -a update -d "Update the trunk branch" -f
